spring.application.name=demo-bai7-docker

# URI mặc định khi chạy ./mvnw spring-boot:run trên máy (Mongo localhost).
# Khi chạy Docker Compose:
#   TH1 (docker-compose.yml)           → host = mongo
#   TH2 (docker-compose.host-mongo.yml) → host = host.docker.internal
# Biến SPRING_DATA_MONGODB_URI ghi đè URI này — không bake secret vào image.
spring.data.mongodb.uri=mongodb://root:DBVWiYdDoMnfWmK@localhost:27017/db_java_t3h_module3_bai_7?authSource=admin

spring.thymeleaf.cache=false

app.movies.page-size=9
app.movies.chart-min-count=2
app.movies.chart-limit=10
app.admin.page-size=10
