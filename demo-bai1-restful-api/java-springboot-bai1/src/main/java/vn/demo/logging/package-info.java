/**
 * §4 Logging — cấu hình mức log + filter minh họa request log.
 *
 * <p>Cấu hình chính nằm ở {@code application.properties}:
 * {@code logging.level.root}, {@code logging.level.vn.demo}.</p>
 *
 * <p>Code nghiệp vụ (EmployeeService, WelcomeEmailService) dùng SLF4J
 * {@code log.debug/info/warn/error} — xem thêm trong package {@code employee} / {@code async}.</p>
 */
package vn.demo.logging;
